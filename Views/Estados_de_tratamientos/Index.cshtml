@model IEnumerable<ProyectoHogarDulceCorazonJoelGramajo.Models.Estados_de_tratamientos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear nuevo registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Estado tratamiento
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_estado_tratamiento)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Estado_de_tratamiento }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.Estado_de_tratamiento }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Estado_de_tratamiento })
        </td>
    </tr>
}

</table>
