@model IEnumerable<ProyectoHogarDulceCorazonJoelGramajo.Models.Géneros>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreGenero)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreGenero)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Género }) |
            @Html.ActionLink("Details", "Details", new { id=item.Género }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Género })
        </td>
    </tr>
}

</table>
