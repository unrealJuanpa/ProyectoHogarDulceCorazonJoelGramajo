@model IEnumerable<ProyectoHogarDulceCorazonJoelGramajo.Models.Géneros>

@{
    ViewBag.Title = "Administración de géneros";
}

<h2>Administración de géneros</h2>

<p>
    @Html.ActionLink("Crear nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Nombre Género
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreGenero)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.Género }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.Género }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.Género })
        </td>
    </tr>
}

</table>
